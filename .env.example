# PostgreSQL (Neon) — use pooled URL for DATABASE_URL, direct URL for DIRECT_URL
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."

# Gmail OAuth (stub — fill in when wiring up)
GMAIL_CLIENT_ID=""
GMAIL_CLIENT_SECRET=""
GMAIL_REDIRECT_URI="http://localhost:3000/api/auth/gmail/callback"

# LinkedIn OAuth (stub — fill in when wiring up)
LINKEDIN_CLIENT_ID=""
LINKEDIN_CLIENT_SECRET=""
LINKEDIN_REDIRECT_URI="http://localhost:3000/api/auth/linkedin/callback"
